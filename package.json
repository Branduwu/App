{
  "name": "litelement",
  "version": "1.0.0",
  "description": "Proyecto de tienda de pasteles usando LitElement",
  "main": "src/main.js",
  "type": "module",
  "scripts": {
    "start": "web-dev-server --config web-dev-server.config.mjs --open",
    "test": "web-test-runner --config web-test-runner.config.mjs \"test/**/*.test.js\" --playwright --browsers chromium firefox webkit"
  },
  "dependencies": {
    "lit": "^2.8.0"
  },
  "devDependencies": {
    "@open-wc/testing": "^3.2.2",
    "@web/dev-server": "^0.1.38",
    "@web/dev-server-esbuild": "^0.2.0",
    "@web/test-runner": "^0.13.31",
    "@web/test-runner-commands": "^0.5.0",
    "@web/test-runner-playwright": "^0.9.0"
  }
}
